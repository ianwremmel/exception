{
  "name": "@ianwremmel/exception",
  "version": "0.0.0-development",
  "description": "Base-class for browser and nodejs Exceptions",
  "main": "src/index.js",
  "repository": {
    "type": "git",
    "url": "https://github.com/ianwremmel/exception.git"
  },
  "scripts": {
    "coveralls": "nyc report --reporter=text-lcov | coveralls",
    "lint": "npm-run-all lint:js lint:types lint:changelog",
    "lint:changelog": "conventional-changelog-lint --from origin/master --to HEAD",
    "lint:commitmsg": "conventional-changelog-lint -e",
    "lint:eslint": "eslint --ignore --ignore-path .gitignore",
    "lint:js": "npm run --silent lint:eslint -- .",
    "lint:staged": "lint-staged",
    "lint:types": "tsc",
    "mocha-reporter-options": "[ -n \"${CI}\" ] && echo '--reporter xunit --reporter-options output=reports/junit/test.xml'",
    "test": "nyc --reporter=text mocha $(npm run --silent mocha-reporter-options) './src/**/*-spec.js'",
    "semantic-release": "semantic-release"
  },
  "engines": {
    "node": ">=10"
  },
  "keywords": [],
  "author": "Ian W. Remmel <design@ianwremmel.com>",
  "license": "MIT",
  "dependencies": {},
  "devDependencies": {
    "@ianwremmel/eslint-config-base": "^1.0.0",
    "@ianwremmel/eslint-config-standard": "^1.0.0",
    "@types/chai": "^4.1.4",
    "@types/mocha": "^5.2.4",
    "chai": "^4.1.2",
    "condition-circle": "^2.0.0",
    "conventional-changelog": "^3.0.4",
    "conventional-changelog-lint": "^2.1.1",
    "coveralls": "^3.0.2",
    "eslint": "^6.0.1",
    "eslint-plugin-import": "^2.13.0",
    "eslint-plugin-mocha": "^6.0.0",
    "husky": "^4.0.0",
    "lint-staged": "^9.0.1",
    "mocha": "^7.0.0",
    "npm-run-all": "^4.1.3",
    "nyc": "^15.0.0",
    "semantic-release": "^16.0.0",
    "typescript": "^3.1.1"
  },
  "lint-staged": {
    "*.js": "npm run lint:eslint --"
  },
  "release": {
    "verifyConditions": "condition-circle"
  },
  "husky": {
    "hooks": {
      "commit-msg": "npm run --silent lint:commitmsg",
      "pre-commit": "npm run --silent lint:staged"
    }
  }
}
